# Ejercicio 13: Crear una cadena de caracteres multilínea

cadena = 'Modules are jars in which some packages are made inaccessible. Modules were used to split JDK 9 and hide ' \
         'the internal code from applications. There is no requirement to package applications into modules, ' \
         'but when it makes sense, modules can be used as an additional obstacle to the code reuse. '

cadena2 = """Modules are jars in which some packages are made inaccessible. Modules were used to split JDK 9 and 
hide the internal code from applications. There is no requirement to package applications into modules, but  
it makes sense, modules can be used as an additional obstacle to the code reuse."""

print(cadena)
print(cadena2)